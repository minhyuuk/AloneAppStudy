<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Practice.FragmentActivity"></activity>
        <activity android:name=".checkmyskill.OpenUrl">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Practice.Intent2" />
        <activity android:name=".Practice.Intent1" />
        <activity android:name=".Practice.Calculator"></activity>
        <activity android:name=".Practice.Listener" />
        <activity android:name=".Practice.MainActivity" />
    </application>

</manifest>